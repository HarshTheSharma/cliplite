{
  "game_id": "apex",
  "name": "Apex Legends",
  "log_paths": [
    "%userprofile%/Saved Games/Respawn/Apex/Logs"
  ],
  "_setup_note": "Path is a directory — ClipLite automatically monitors the most recently modified file inside it (Apex rotates log names per session). No game-side setup required.",
  "events": [
    {
      "event": "kill",
      "match": "\\[Client\\].*kill",
      "regex": true,
      "_note": "Apex log kill format is not publicly documented and changes between patches. Tune this regex against your own logs."
    },
    {
      "event": "death",
      "match": "\\[Client\\].*downed|\\[Client\\].*respawn",
      "regex": true
    }
  ],
  "monitor_mode": "tail",
  "poll_ms": 250
}
