{
  "game_id": "valorant",
  "name": "VALORANT",
  "log_paths": [
    "%localappdata%/VALORANT/Saved/Logs/VALORANT.log"
  ],
  "_setup_note": "No game-side setup required. VALORANT.log is reset on each game launch, so ClipLite always attaches at EOF and only sees events from the current session.",
  "events": [
    {
      "event": "kill",
      "match": "LogKillfeedController.*kill",
      "regex": true,
      "_note": "VALORANT.log kill feed entries are prefixed with LogKillfeedController. Adjust the regex if Riot changes the log category name."
    },
    {
      "event": "death",
      "match": "LogKillfeedController.*died|LogDeathmatch.*died",
      "regex": true
    }
  ],
  "monitor_mode": "tail",
  "poll_ms": 200
}
