{
  "game_id": "r6siege",
  "name": "Rainbow Six Siege",
  "log_paths": [
    "%documents%/My Games/Rainbow Six Siege/GameSettings.ini",
    "%localappdata%/Ubisoft Game Launcher/logs/launcher_log.txt"
  ],
  "_setup_note": "R6 Siege does not expose a reliable kill/death log via accessible files. The paths here are best-effort. For accurate detection, use the Overwolf/Tracker Network overlay which writes a structured event log — update log_paths to that output file and tune the match strings accordingly.",
  "events": [
    {
      "event": "kill",
      "match": "KillEnemy|PlayerKilled",
      "regex": true
    },
    {
      "event": "death",
      "match": "LocalPlayerDied|PlayerDowned",
      "regex": true
    }
  ],
  "monitor_mode": "tail",
  "poll_ms": 300
}
